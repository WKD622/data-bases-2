%##noBuild

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[MeX]{polski}

\usepackage[a4paper, margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{pdflscape}
\usepackage{lmodern}
\usepackage[hidelinks]{hyperref}
\usepackage[toc,page]{appendix}
\usepackage{calc}
\usepackage[nomessages]{fp}

\renewcommand{\appendixtocname}{Dodatki}
\renewcommand{\appendixpagename}{Dodatki}
\setlength{\parindent}{0pt}
\lstdefinestyle{tsql}{
	language=SQL,
	inputencoding=utf8,
	extendedchars=true,
	breaklines=true,
	breakatwhitespace=true,
	basicstyle=\ttfamily,
	numbers=left,
	numbersep=5pt,
	tabsize=4,
	frame=single,
	stepnumber=1,
	firstnumber=1,
	showstringspaces=false,
	numberstyle=\scriptsize\color{gray},
	commentstyle=\color{gray},%\color{purple!40!black},
	identifierstyle=\color{black},
	stringstyle=\color{orange},
	% constructions
	keywordstyle=[0]\bfseries\color{green!50!black},
	keywords=[0]{function, returns, declare, if, is, return,
		create, view, select, group, order, by, as, from, update,
		delete, set, where, begin, end, top, output, procedure, table,
		primary, key, foreign, constraint, references, check, unique,
		identity, default, commit, transaction, xact_abort, while,
		insert, into, values, exec, else, type, rollback, for, trigger,
		clustered, index},
	% functions
	keywordstyle=[1]\bfseries\color{blue},
	keywords=[1]{getdate, isnull, dateadd, week, raiserror, scope_identity,
		sum, avg, count, mx, min},
	% types
	keywordstyle=[2]\color{purple},
	keywords=[2]{null, int, date, time, decimal, nvarchar, varchar, char,
		money, varbinary, bit},
	% operators
	keywordstyle=[3]\color{gray!90!black},
	keywords=[3]{and, or, left, right, inner, outer, join, on, not, dbo, in,
		exists, inserted, deleted},
	% symbols
	otherkeywords={;,[,],[(],[)],*},
	keywordstyle=[4]\color{black},
	keywords=[4]{;,[,],[(],[)]},
	keywordstyle=[5]\color{orange!50!black},
	keywords=[5]{*},
	% variables
	%moredelim=*[s][\color{orange!80!black}]{@}{\ },
	sensitive=true
}

\lstset{literate=
 {ś}{{\'s}}1 {ą}{{\k{a}}}1 {ę}{{\k{e}}}1 {ł}{{\l}}1
 {ć}{{\'c}}1 {ń}{{\'n}}1 {ó}{{\'o}}1 {ź}{{\'z}}1
 {ż}{{\.z}}1
}
\lstset{style=tsql}
